// This layout is intentionally empty - it just needs to exist
// to create a separate layout group for the studio
export default function StudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
